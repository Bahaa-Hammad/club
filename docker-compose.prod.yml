version: '3.8'

services:
  web:
    build:
      context: ./ieee
      dockerfile: Dockerfile.prod
    image: 481243181382.dkr.ecr.eu-north-1.amazonaws.com/ieee-ec2:web
    command: gunicorn club.wsgi:application --bind 0.0.0.0:8000
    logging:
      driver: "awslogs"
      options:
          awslogs-region: "us-east-1"
          awslogs-group: "ieee-ec2"
          awslogs-stream: "ieee-ec2"
    volumes:
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
    expose:
      - 8000
    env_file:
      - ./.env.prod
    depends_on:
      - postgres-database
      
  postgres-database:
    image: postgres:13.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.prod.db

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    image: 481243181382.dkr.ecr.eu-north-1.amazonaws.com/ieee-ec2:nginx
    restart: always
    volumes:
      - static_volume:/home/app/web/staticfiles
      - media_volume:/home/app/web/mediafiles
    ports:
      - 1337:80
    depends_on:
      - web

volumes:
  postgres_data:
  static_volume:
  media_volume:
